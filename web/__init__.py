"""Browser front end for TradingAgents (FastAPI + a single static page).

Run locally with ``python -m web`` and open http://localhost:8501, or expose it
across a tailnet with ``scripts/serve_tailscale.sh``.
"""
